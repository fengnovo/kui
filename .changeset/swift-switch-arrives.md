---
"@fengnovo/kui": minor
---

新增 `Switch` 开关组件:支持受控/非受控(`checked` / `defaultChecked` / `onCheckedChange`)、`sm/md/lg` 尺寸变体、`disabled` 态;交互逻辑沉淀到可脱离 DOM 单测的 Headless 内核 `useSwitch`;渲染为原生 `<button role="switch">`,键盘可达、axe 无违规。提供独立入口 `@fengnovo/kui/switch` 支持按需引入。
